// Route file. The screen lives in @/features/customer/screens/emergency/service.
//
// Customer route group (§5.1.5). Nothing here may import from features/provider — the boundaries lint rule fails CI if it does.
export { default } from '@/features/customer/screens/emergency/service';
