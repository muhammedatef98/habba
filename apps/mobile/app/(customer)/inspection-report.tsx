// Route file. The screen lives in @/features/customer/screens/inspection-report.
//
// The READING side of an inspection. The provider's capture screen is
// (provider)/inspection.tsx — the two never import each other (§5.1.5); they
// share `inspection_reports` and the renderer types in @habba/core.
export { default } from '@/features/customer/screens/inspection-report';
