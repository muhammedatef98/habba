// Route file. The screen lives in src/features/provider/screens/job.
//
// Provider route group (§5.1.5), reachable only through the mode switcher
// and only for an approved provider role — see (provider)/_layout.tsx.
export { default } from '@/features/provider/screens/job';
