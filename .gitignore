node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# Expo
.expo/
.expo-shared/
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Generated native projects. `expo run:ios` / `expo prebuild` recreate these
# from app.json — 1.2 GB of CocoaPods and Xcode state that must not be
# versioned. metro.config.js and app.json ARE the source of truth.
apps/*/ios/
apps/*/android/

# Local database harness (see supabase/scripts/local-db.sh)
supabase/.data/
supabase/.tmp/

# Secrets — never commit. CLAUDE.md §11.
.env
.env.*
!.env.example

.DS_Store
*.log

# Integration test artifacts (rendered report pages)
.tmp/
